This chapter prepares the ground for the RAG-oriented AI-Q blueprint deployment.

The first task is to verify that Kubernetes is reachable. After that, the tutorial will layer in the AI-Q chart deployment commands.

## What to check first

- The Kubernetes API responds.
- The node is Ready.
- Helm is available.

The next lesson runs those checks directly against the launchable cluster.
