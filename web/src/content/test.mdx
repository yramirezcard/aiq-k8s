This lesson is the placeholder for post-deployment validation.

Once the AI-Q deployment lesson is filled in, this page will verify the services, pods, and exposed endpoints.

Start with the namespace-level view:

```bash
kubectl get pods -A
```

Later, this page will narrow that down to the AI-Q namespace and include an application-level smoke test.
