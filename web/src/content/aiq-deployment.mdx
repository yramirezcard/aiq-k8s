This lesson is the placeholder for the AI-Q Helm deployment walkthrough.

The commands will be added here as the tutorial content grows. The intended flow is:

1. Clone or locate the official AI-Q repository.
2. Create the namespace for the blueprint.
3. Create the required Kubernetes secrets.
4. Update Helm dependencies.
5. Install the chart from `deploy/helm/deployment-k8s`.

For now, confirm the cluster context before moving on:

```bash
kubectl config current-context
kubectl get namespaces
```
