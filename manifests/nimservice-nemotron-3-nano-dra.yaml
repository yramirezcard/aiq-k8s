# Self-hosted Nemotron NIM for Part IV of the AI-Q lab.
#
# This manifest expects the NIM Operator CRDs, NVIDIA GPU DRA driver, and the
# NGC pull/auth secrets to already exist. It uses DRA auto-creation so the
# NIMService creates its ResourceClaimTemplate from spec.draResources.

apiVersion: apps.nvidia.com/v1alpha1
kind: NIMCache
metadata:
  name: nemotron-3-nano-30b-a3b
  namespace: nim-service
spec:
  source:
    ngc:
      modelPuller: nvcr.io/nim/nvidia/nemotron-3-nano:2.0.8
      pullSecret: ngc-secret
      authSecret: ngc-api-secret
      model:
        engine: vllm
        tensorParallelism: "1"
  storage:
    pvc:
      create: true
      storageClass: ""
      size: "120Gi"
      volumeAccessMode: ReadWriteOnce
---
apiVersion: apps.nvidia.com/v1alpha1
kind: NIMService
metadata:
  name: nemotron-3-nano
  namespace: nim-service
spec:
  image:
    repository: nvcr.io/nim/nvidia/nemotron-3-nano
    tag: 2.0.8
    pullPolicy: IfNotPresent
    pullSecrets:
      - ngc-secret
  authSecret: ngc-api-secret
  storage:
    nimCache:
      name: nemotron-3-nano-30b-a3b
      profile: "352d88f8021d0ce396a61d10e929d1d4b45f75038b593595d0d92f80a398a032"
  replicas: 1
  draResources:
    - claimCreationSpec:
        devices:
          - name: gpu
            deviceClassName: gpu.nvidia.com
            driverName: gpu.nvidia.com
            capacitySelectors:
              - key: memory
                op: GreaterThanOrEqual
                value: 80Gi
  expose:
    service:
      type: ClusterIP
      port: 8000
